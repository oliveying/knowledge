{
  "name": "node",
  "version": "1.0.0",
  "description": "> auth duruo",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy": "gh-pages -d dist",
    "live-serve": "live-server ./dist --port=8081",
    "deploy:build": "npm run docs:build && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oliveying/node.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oliveying/node/issues"
  },
  "homepage": "https://github.com/oliveying/node#readme",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "live-server": "^1.2.1",
    "vuepress": "^0.14.10"
  },
  "resolutions": {
    "webpack-dev-middleware": "3.6.0"
  },
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^1.0.0-alpha.0",
    "async": "^3.2.0",
    "gh-pages": "^2.0.1"
  }
}
